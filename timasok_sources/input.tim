def main(  ){  

    res = akkerman(2,3);
    print(res);

};

def akkerman(n,m)
{
    if(n<0)
    {
        print_line(error);
    };

    if(m<0)
    {
        print_line(error);
    };

    if(n)
    {}else 
    {
        new_m = m+1;
        return(new_m);
    };

    if(m)
    {}else 
    {
        x = akkerman(n-1,1);
        return(x);
    };

    new_m = akkerman(n,m-1);
    x = akkerman(n, new_m);
    return(x);    
};